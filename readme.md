<!-- To run the application use the following steps -->
1. python -m venv .venv
2.  .\.venv\Scripts\activate
3. pip install -r requirements.txt
4. uvicorn app.api:app
5. write your question to get the output
